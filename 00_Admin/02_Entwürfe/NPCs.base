views:
  - type: cards
    name: Table
    filters:
      and:
        - file.tags.contains("NPC")
        - "!Fraktionen.isEmpty()"
    groupBy:
      property: Fraktionen
      direction: ASC
    order:
      - file.name
      - Rolle
    cardSize: 190
